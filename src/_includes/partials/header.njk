<header class="w-full px-6 py-6 bg-slate-100">
    <nav class="container flex justify-between mx-auto">

    <h1 class="text-xl font-semibold"><a href="{{ '/' | url }}">{{ metadata.title }}</a></h1>

    {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
    {# <div class="items-center gap-8 md:flex">
        <ul class="nav">
        {%- for entry in collections.all | eleventyNavigation %}
            <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
        {%- endfor %}
        </ul>
    </div> #}
    
    <div class="items-center hidden gap-8 md:flex">
      {%- for entry in collections.all | eleventyNavigation %}
        <a
          class="{{ 'text-indigo-600' if page.url == item.route }} font-semibold hover:text-indigo-600 text-gray-600"
          href="{{ entry.url | url }}">{{ entry.title }}</a>
      {% endfor %}
    </div>



    </nav>
</header>